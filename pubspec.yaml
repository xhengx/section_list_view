name: section_list_view
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sticky_header_footer: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

